interface:
  display_name: "GEE Agent Skill"
  short_description: "Traceable Google Earth Engine workflows"
  default_prompt: "Use $gee-agent-skill to plan, ground, render, and validate a reproducible Earth Engine workflow before any confirmed live export."
